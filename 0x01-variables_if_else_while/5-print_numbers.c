#include <stdio.h>
#include <stdlib.h>

/**
 * main - Prints the alphabet without q and e.
 *
 * Return: Always return 0 
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
