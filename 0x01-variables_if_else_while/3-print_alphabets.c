#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 *
 * Return: Always return 0
 */
int main(void)
{
	int n = 97;
	while (n < 123)
	{
		putchar(n);
		n++;
	}
	return (0);
}
